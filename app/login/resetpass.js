import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import { ResetPassBody } from '../../components'

const Resetpass = () => {
    return (
        <ResetPassBody />
    )
}

export default Resetpass

const styles = StyleSheet.create({})