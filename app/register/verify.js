import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import { VerifyBody } from '../../components'

const Verify = () => {
    return (
        <VerifyBody />
    )
}

export default Verify

const styles = StyleSheet.create({})