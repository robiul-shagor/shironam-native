import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import { SetInterestBody } from '../../components'

const Interests = () => {
    return (
        <SetInterestBody />
    )
}

export default Interests

const styles = StyleSheet.create({})